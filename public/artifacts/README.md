Drop redacted screenshots here, then reference them from `caseStudies[<id>].artifacts`
in data/content.js:

  artifacts: [
    { src: '/artifacts/wazuh-dashboard.png', alt: 'Wazuh dashboard — agent fleet overview' },
  ]

Before uploading anything, redact: real hostnames, public IPs, usernames,
tokens, agent keys, and any address outside RFC1918. Lab IPs (192.168.x.x,
10.x.x.x) are fine.
